<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" /> <!-- Android 13+ 读取图片的专用权限 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" /> <!-- 如果是 Android 12 及以下，依然需要旧权限 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DunXiaWeather"
        tools:targetApi="31" >
        <activity
            android:name="Layout.FrameLayoutActivity"
            android:exported="false" />
        <activity
            android:name="Layout.ContstrantLayoutActivity"
            android:exported="false" />
        <activity
            android:name="PlayMedia.PlayMediaActivity"
            android:exported="false" />
        <activity
            android:name="HomeDetail.HomeContentDetailActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DunXiaWeather.Starting" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>